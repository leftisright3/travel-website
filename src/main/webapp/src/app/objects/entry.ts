import {Section} from "./section";

export class Entry {
  name?: string;
  sections?: Section[];
  location?: string;
  pictures?: string;
  latitude?: number;
  longitude?: number;
  image?: string;
  title?: string;
  heading?: string;
  description?: string;
}
